# PR #60347 在 2.1.0 分支的 **BE 反合（backport）指南**

> 目标：仅反合 PR #60347 的 **BE 端**能力（更多 INTERVAL 复合 time-unit 支持），不引入 FE 大规模语法/函数类变更。

## 1. 先明确：2.1.0 做 BE-only 反合的边界
PR #60347 原始变更包含 FE + BE + 测试三部分；2.1.0 如果只做 BE 端，建议按“最小可用、最小风险”原则：

- 仅引入 BE 的时间单位解析与计算能力。
- FE 侧先不扩展 Nereids 新语法（避免 2.1.0 稳定分支放大改动面）。
- 通过已有可下推路径（或常量折叠后的执行路径）先验证 BE 逻辑正确性。

## 2. PR #60347 的 BE 端关键文件（优先关注）
根据 upstream PR 文件树，BE 主要涉及以下文件：

- `be/src/vec/functions/date_format_type.h`
- `be/src/vec/functions/date_time_transforms.h`
- `be/src/vec/functions/datetime_errors.h`
- `be/src/vec/functions/function_date_or_datetime_computation.cpp`
- `be/src/vec/functions/function_date_or_datetime_computation.h`
- `be/src/vec/functions/function_date_or_datetime_to_string.cpp`
- `be/src/vec/runtime/vdatetime_value.cpp`
- `be/test/vec/function/function_time_test.cpp`

## 3. 建议反合顺序（2.1.0）

### Step A：先拿提交，后拆分（推荐）
先在本地加 upstream，再拉取 PR 对应提交：

```bash
git remote add upstream https://github.com/apache/doris.git
git fetch upstream pull/60347/head:pr60347
```

然后在 `origin/2.1.0`（或你的维护分支）上分步挑 BE 变更：

```bash
git checkout -b backport/pr60347-be origin/2.1.0
# 先尝试主提交（功能主体）
git cherry-pick -n 8785708
# 再补样式/编译修复（按需）
git cherry-pick -n 1931daf
git cherry-pick -n 857c32b
```

> `-n`（--no-commit）是为了把 FE 改动剥离掉，只保留 BE 相关文件后再一次性提交。

### Step B：剥离 FE，仅保留 BE
执行后把 FE 文件回退，只保留第 2 节 BE 路径：

```bash
git restore --staged .
git restore .
# 重新只加入 BE 文件
git add be/src/vec/functions/date_format_type.h \
        be/src/vec/functions/date_time_transforms.h \
        be/src/vec/functions/datetime_errors.h \
        be/src/vec/functions/function_date_or_datetime_computation.cpp \
        be/src/vec/functions/function_date_or_datetime_computation.h \
        be/src/vec/functions/function_date_or_datetime_to_string.cpp \
        be/src/vec/runtime/vdatetime_value.cpp \
        be/test/vec/function/function_time_test.cpp
```

如果某些文件在 2.1.0 不存在/结构不同，优先把逻辑落到：

1. `function_date_or_datetime_computation.*`（计算主路径）
2. `vdatetime_value.cpp`（底层时间值处理）
3. `date_format_type.h`（单位枚举/类型映射）

### Step C：处理 2.1.0 常见冲突点

1. **枚举或函数签名差异**：
   - 2.1.0 与 master 的 enum/模板参数可能不一致。
   - 做法：保持 2.1.0 既有接口，移植“单位解析 + 数值拆分 + 计算分支”逻辑，不强行对齐 master 的接口形态。

2. **微秒相关分支**：
   - `DAY_MICROSECOND`、`HOUR_MICROSECOND`、`MINUTE_MICROSECOND`、`SECOND_MICROSECOND` 需要核对精度和进位。
   - 做法：统一复用 2.1.0 现有 datetime 精度处理函数，避免重复造轮子。

3. **错误码/报错文案**：
   - `datetime_errors.h` 在不同分支可能有常量差异。
   - 做法：优先复用 2.1.0 既有错误码，仅补最小新增项。

## 4. 最小回归清单（BE-only）
反合后至少跑以下检查：

1. BE 单测：
   - `be/test/vec/function/function_time_test.cpp` 覆盖新增单位。
2. SQL 端到端抽样（重点）：
   - `DATE_ADD/DATE_SUB`：`YEAR_MONTH`、`DAY_HOUR`、`DAY_MINUTE`、`DAY_SECOND`、`DAY_MICROSECOND`
   - `EXTRACT`：`HOUR_MINUTE`、`HOUR_SECOND`、`MINUTE_SECOND`、`SECOND_MICROSECOND`
3. 边界值：
   - 负间隔、跨天/月/年、`23:59:59.999999`。

## 5. 提交建议
建议拆成 2 个 commit，便于 review 与回滚：

- Commit 1：BE 功能移植（核心逻辑）
- Commit 2：BE 测试补齐（含必要修正）

示例提交信息：

- `backport(2.1): support more interval composite units in BE`
- `test(2.1): add interval composite unit tests for BE`

## 6. 一句话结论
2.1.0 的“反合这部分代码”建议做 **BE-only 精准移植**：按 `8785708 -> 1931daf -> 857c32b` 提交顺序抽取逻辑，但最终仅保留 BE 文件并补齐 BE 回归，这样风险最小、交付最快。
